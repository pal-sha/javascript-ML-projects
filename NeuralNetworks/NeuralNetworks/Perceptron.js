class Perceptron {

    this.weight = [2];
    Perceptron() {
        for (let i = 0; i < this.weights.length; i++) {
            this.weights[i] = Math.random() * 2 - 1;
        }
    }

    int sign(float n) {
        if (n >= 0) {
            return 1;
        }
}
}